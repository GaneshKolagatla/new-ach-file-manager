spring.application.name=AlacritiAchGenerator
# JDBC URL (no user/password in URL)
spring.datasource.url=jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:5432/postgres?user=postgres.muuxfodsbqaxapdqfztu&password=SUPABASE@123
# Credentials (separate)
spring.datasource.username=postgres
spring.datasource.password=SUPABASE@123

# Required additional configs
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Root log level
logging.level.root=INFO

# Log level for your package
logging.level.com.alacriti=DEBUG

# Create new log file on every run using UUID
logging.file.name=logs/app-${random.uuid}.log

# Log format for file
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
